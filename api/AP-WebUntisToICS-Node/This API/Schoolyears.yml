info:
  name: Schoolyears
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/schoolyears"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
