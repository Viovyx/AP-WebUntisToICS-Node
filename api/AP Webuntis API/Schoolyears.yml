info:
  name: Schoolyears
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/schoolyears"
  headers:
    - name: anonymous-school
      value: ap
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
